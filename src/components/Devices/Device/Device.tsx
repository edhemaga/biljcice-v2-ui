import { FC } from "react";
import { IDevice } from "../../../shared/components/Device/IDevice";

const Device: FC<{ data: Partial<IDevice>[] }> = () => {
  return <div>Device!</div>;
};

export default Device;
